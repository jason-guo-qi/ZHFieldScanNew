<Window x:Class="FieldScanNew.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
        xmlns:viewmodels="clr-namespace:FieldScanNew.ViewModels"
        mc:Ignorable="d"
        Title="FieldScan Pro" Height="720" Width="1280"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="5" CaptionHeight="32" CornerRadius="0" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A3E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#CCCCCC" BorderThickness="1" Background="White">
        <DockPanel>
            <Grid DockPanel.Dock="Top" Height="32" Background="#007ACC">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                    <TextBlock Text="FieldScan Pro v1.0" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="—" Click="MinimizeButton_Click" Style="{StaticResource TitleBarButtonStyle}"/>
                    <Button Content="☐" Click="MaximizeButton_Click" Style="{StaticResource TitleBarButtonStyle}"/>
                    <Button Content="✕" Click="CloseButton_Click" Style="{StaticResource CloseButtonStyle}"/>
                </StackPanel>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="280" MaxWidth="500"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <DockPanel Grid.Column="0" Background="#F0F0F0">
                    <Grid DockPanel.Dock="Top" Margin="10,10,10,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="测试任务" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="12" Width="24" Height="24" ToolTip="新建项目" Command="{Binding AddNewProjectCommand}" Margin="0,0,5,0"/>
                            <Button Content="&#xE8DE;" FontFamily="Segoe MDL2 Assets" FontSize="12" Width="24" Height="24" ToolTip="加载项目" Command="{Binding LoadProjectCommand}"/>
                        </StackPanel>
                    </Grid>

                    <TreeView x:Name="TaskTreeView" Margin="5" Background="Transparent" BorderThickness="0" ItemsSource="{Binding Projects}">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="True"/>
                                <Setter Property="Padding" Value="2"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                        <TreeView.Resources>
                            <HierarchicalDataTemplate DataType="{x:Type viewmodels:ProjectViewModel}" ItemsSource="{Binding Measurements}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Orange"/>
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="Bold"/>
                                    <StackPanel.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="新建测量项" Command="{Binding AddNewMeasurementCommand}"/>
                                        </ContextMenu>
                                    </StackPanel.ContextMenu>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type viewmodels:MeasurementViewModel}" ItemsSource="{Binding Steps}">
                                <TextBlock Text="{Binding DisplayName}">
                                    <TextBlock.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="重命名" Command="{Binding RenameCommand}"/>
                                        </ContextMenu>
                                    </TextBlock.ContextMenu>
                                </TextBlock>
                            </HierarchicalDataTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:InstrumentSetupViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="30,0,5,0" Foreground="DimGray"/>
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:ProbeSetupViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE771;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="30,0,5,0" Foreground="DimGray"/>
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:ScanSettingsViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="30,0,5,0" Foreground="DimGray"/>
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:ZCalibViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE752;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="30,0,5,0" Foreground="DimGray"/>
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:XYCalibViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE809;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="30,0,5,0" Foreground="DimGray"/>
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:ScanAreaViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE7B3;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="30,0,5,0" Foreground="DimGray"/>
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </StackPanel>
                            </DataTemplate>
                        </TreeView.Resources>
                    </TreeView>
                </DockPanel>

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#EAEAEA"/>

                <Grid Grid.Column="2">
                    <DockPanel>
                        <ToolBar DockPanel.Dock="Top">
                            <Button Content="▶ 开始扫描" Command="{Binding StartScanCommand}" IsEnabled="{Binding !IsScanning}"/>
                            <Button Content="▶ QBC扫描" Command="{Binding QBCStartScanCommand}" IsEnabled="{Binding !IsScanning}"/>
                            <Button Content="■ 停止扫描" Command="{Binding StopScanCommand}" IsEnabled="{Binding IsScanning}"/>
                        </ToolBar>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="250"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="10">
                                <oxy:PlotView Model="{Binding HeatmapModel}" Background="Transparent"/>
                            </Grid>

                            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#EAEAEA"/>

                            <TabControl Grid.Row="2" Margin="10">
                                <TabItem Header="Log">
                                    <TextBox IsReadOnly="True" VerticalScrollBarVisibility="Auto" Text="{Binding LogOutput}"/>
                                </TabItem>
                                <TabItem Header="频谱图">
                                    <oxy:PlotView Model="{Binding SpectrumModel}"/>
                                </TabItem>
                            </TabControl>
                        </Grid>
                    </DockPanel>
                </Grid>
            </Grid>
        </DockPanel>
    </Border>
</Window>